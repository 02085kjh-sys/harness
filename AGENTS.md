# 밭매고 · 밭매고맵 — 에이전트 공통 규칙

## 프로젝트

- **밭매고** (batmaego.work): 라이브 베타. 지번별 농지 가격 · 토양 · 작목 추천(34종 ROI) · 시세 · 지원사업 · 날씨.
  공공 데이터 연동: 국토부 브이월드, 농진청 흙토람 · 소득자료집, KAMIS, 보조금24, 기상청.
- **밭매고맵**: 시제품. 지번 검색 → 지적 경계 표시 · 수정 → 하우스/두둑 자동 배치 → 동수 · 면적 · 심는 면 비율 계산 → 방향별 비교 → 3D 확인.
  Leaflet + Geoman. 브이월드 WFS를 Fastify 프록시로 수신. EPSG:4326 → 5179 변환 후 5cm 정수 격자 계산.
  배치 엔진은 난수 · 현재시각 · 네트워크 호출을 lint로 차단해 결정론 보장. 소스 약 2만 1천 줄, 테스트 1,118개.
- **팀**: 한국농수산대학교 재학생 3명 (개발 / 기획 / 현장 실측 · 사용자 테스트)

## 지키는 원칙 (절대 어기지 않기)

1. **데이터 없으면 없다고 표시.** 가짜 값 채우지 않기. 조회 실패는 실패라고 그대로 알리기.
2. **모든 숫자에 출처 · 기준일 붙이기.** 출처 없는 값은 프리셋 등록 자체가 안 되게.
3. **지적 경계는 "참고용 근사"로 항상 표시.** 확정 면적은 지적측량으로 넘기기.
4. **배치 계산은 사용자 브라우저에서만.** 농지 정보를 서버에 남기지 않기.
5. **같은 입력 → 같은 결과.** 배치 엔진 안에서 난수 · 현재시각 · 네트워크 호출 금지.
6. **화면 말은 현장 말로.** '피복면적' 대신 '심는 면', '필지' 대신 '밭'.
7. **환경 정보(해 · 그림자 · 바람 · 경사)는 표시 전용.** 동수 계산에 몰래 개입하지 않기.

## 역할 분담

| 역할 | 맡는 일 |
|---|---|
| **설계 · 검토 (메인 모델)** | 아키텍처 결정, 코드 · 문서 리뷰, 배치 알고리즘 정확성 판단, 테스트 통과 여부 게이트, "이게 맞나" 판단. **직접 구현하지 않음.** |
| **Opus** | 여러 모듈이 얽힌 구현, 좌표/GIS 계산 수정, 사업계획서 · 지원서 본문 작성 |
| **Sonnet** | 단일 파일 수정, UI 문구 교체, 테스트 케이스 추가, 마크다운 정리, 데이터 매핑, 반복 작업 |

위임할 때는 "무엇을, 어떤 파일에서, 완료 기준이 뭔지"를 한 문단으로 넘긴다.
위임 결과가 돌아오면 검토하고 통과/반려만 결정한다. 직접 다시 짜지 않는다.

## Effort 기준

| 작업 종류 | 권장 Effort |
|---|---|
| UI 문구 교체, 마크다운 정리, 데이터 필드 매핑 | low |
| 테스트 케이스 추가, 단일 파일 버그 수정 | low ~ medium |
| 사업계획서 · 지원서 본문, 여러 파일 걸친 기능 추가 | medium |
| 좌표 변환 · 폴리곤 교차 · 결정론 검증 등 배치 엔진 정밀 로직 | medium (검토만 high) |
| 아키텍처 결정, 리뷰, 게이트 판단 | medium |

**high · xhigh는 배치 엔진 정밀 로직 검토 외에는 쓰지 않는다.**
확실하지 않으면 매 작업 착수 전에 "권장 Effort는 ___이고 이유는 ___"를 먼저 제시한다.

## 작업 시작 루틴

각 작업 착수 전에 아래 세 줄만 먼저 보여준다.

1. 이 작업의 목표와 완료 기준
2. 권장 Effort와 이유
3. 어느 모델에게 무엇을 위임할지

## 지금 남은 일

- 팀 외 사용자 대상 사용성 테스트 (한농대 학우 베타)
- 밭매고맵 실측 정확도 검증 (목표 오차 ±5%)
- 공공 공간정보 상업 이용 조건 확인
- 모두의 창업 2차 도전신청서 제출 (9/17 마감)
