# Agent Rules

<!-- 계정 공통 규칙만. 프로젝트별 규칙은 각 프로젝트 스킬에 있다 (thesis-seolhyang / batmaego). -->

## 레인 배치 (오케스트레이션 원칙)

- 메인 에이전트(오케스트레이터)는 **설계·분해·검토·최종 판단**만 맡는다. 논문/문서/코드의 실제 작성, 반복 편집, 자료 수집·정리 같은 실행 작업은 서브에이전트에 위임한다.
  - 설계: 목차·논리 구조·근거 요건·수치 가정 정의, 작업 분해와 인수인계 지시문 작성
  - 검토: 사실·수치 정합성, 합계/수식 재검증, 톤·형식 일관성, 최종 승인
- 실행 레인은 Sonnet을 기본으로 쓴다 (`subagent` 프로필의 `fast`/`deep` 카테고리 = claude-sonnet-5).
  - `fast`(sonnet, low): 단순·기계적 작업 — 자료 수집, 표 채우기, 포맷 정리, 단순 편집
  - `deep`(sonnet, medium): 본문 작성, 섹션 초안, 계산 스크립트 작성 등 실질 산출물
  - 메인(opus/최상위 모델, medium): 설계·검토·통합만. 직접 장문 집필하지 않는다.
- **오버 엔지니어링 금지**: 서브에이전트를 습관적으로 남발하지 않는다. 한 번의 툴 호출이나 5분 이내 단순 작업은 직접 처리하고, 병렬화 이득이 명확할 때만(독립적이고 겹치지 않는 범위) 나눈다.
- 위임할 때는 "무엇을, 어떤 파일에서, 완료 기준이 뭔지"를 한 문단으로 넘긴다. 결과가 오면 검토하고 통과/반려만 결정한다. 직접 다시 짜지 않는다.

## 작업 시작 루틴

각 작업 착수 전에 아래 세 줄만 먼저 보여준다.

1. 이 작업의 목표와 완료 기준
2. 권장 Effort와 이유
3. 어느 모델에게 무엇을 위임할지

## 프로젝트 분리

- 프로젝트별 규칙(도메인 원칙·산출물 기준·형식 규칙)은 **해당 프로젝트 스킬에만** 둔다. 다른 프로젝트 규칙을 끌어오지 않는다.
  - 논문(설향 딸기 영농창업계획): 프로젝트 워크스페이스 `~/Desktop/논문 준비 자료/.agents/skills/thesis-seolhyang/`
  - 뱭매고·뱭매고맵: 계정 스킬 `~/.aside/u/0/skills/user/batmaego/`
- 원본 하네스 저장소: `github.com/02085kjh-sys/harness` (`projects/thesis-seolhyang/`, `projects/batmaego/`)

## Effort(추론 강도) 규칙

- 논문·문서 작업에 high/xhigh는 기본적으로 쓰지 않는다. 기본값은 medium, 단순 작업은 low.
- 판단이 애매하면 **매 작업 착수 전 권장 Effort를 먼저 제시**하고 진행한다. 예: "이 작업은 medium 권장(설계는 medium, 표 채우기는 low). 이대로 갈까?"
- high 이상은 사용자가 명시적으로 승인했을 때만 사용한다.
- 사용량(usage limit)이 빡빡한 상황에서는 Effort를 한 단계 낮추고 위임 비중을 높인다.
